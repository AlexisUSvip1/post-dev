import {makeStyles} from '@mui/styles';
export const useStyles = makeStyles({
  topContainer: {
    position:'absolute',
    left:'30px',
    top:'15px',
    fontSize:'10px',
  },
  topImage: {
      width: '80%',
  },
});